{
  "name": "sprea-edge-api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply sprea-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply sprea-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260827.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.127.0"
  }
}
